<?xml version="1.0" encoding="UTF-8"?><script>from NukeFaceTrack import core, ui
import nuke

# Setup
node = nuke.selectedNode()
proc = core.FaceProcessor()
start = int(nuke.Root()['first_frame'].value())

# 1. Process 20 frames (Fast test)
data = proc.run_range(node, start, start + 20)

# 2. Build the tools
ui.create_all_tools(data, node)</script>
